import React, { useState } from 'react'
import { Navbar, MobileNav, Typography, Button, IconButton, Card, MenuItem } from '@material-tailwind/react';

export default function TopBar() {
    const [openNav, setOpenNav] = useState(false);
   
    return(
        <div>
            <p>TopBar</p>
        </div>
    )
}